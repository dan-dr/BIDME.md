{
  "name": "bidme",
  "version": "0.3.0",
  "module": "index.ts",
  "type": "module",
  "keywords": ["cli", "github", "sponsors", "stripe", "bidding", "readme"],
  "bin": {
    "bidme": "./dist/cli.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target node",
    "cli": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "@clack/core": "^0.4.1",
    "@iarna/toml": "^2.2.5",
    "commander": "^13.1.0"
  }
}
