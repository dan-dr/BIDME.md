name: "BidMe: Process Bid"

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write

jobs:
  process-bid:
    runs-on: ubuntu-latest
    # Only run on issue comments (not PRs), only on bidme-labeled issues,
    # and only when the comment contains a yaml code fence (bid format)
    if: |
      !github.event.issue.pull_request &&
      contains(toJSON(github.event.issue.labels.*.name), 'bidme') &&
      contains(github.event.comment.body, '```yaml')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Process bid comment
        run: bun run src/cli.ts process-bid ${{ github.event.issue.number }} ${{ github.event.comment.id }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
